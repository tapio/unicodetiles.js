General:
	* Split Engine to separate file?
Engine:
	* Investigate if shader callback should be removed
	* Investigate if tile cache usage could be simplified to use only one code path
Input:
	* Key repeat broken on Opera (does not honor the delay)
Viewport:
	* Check for presence of renderers before attempting to create one
	* setDefaultColor, setDefaultBackground
		-> CSS from the parent as is now?
WebGLRenderer:
	* Fix vertical gaps
	* Test falling back
Examples:
	* More GC friendliness
	* Something that demoes WebGL
	* DOMRenderer tile CSS styling (border-radius, drop-shadow etc...)
Tests:
	* More GC friendliness
	* functional-03-inheriting-css-colors

